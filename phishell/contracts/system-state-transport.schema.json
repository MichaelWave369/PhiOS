{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "phios.system-state-transport.v1",
  "title": "PhiOS Unified System State Transport",
  "type": "object",
  "required": [
    "transportSchemaVersion",
    "transport",
    "transportIdentity",
    "localOnly",
    "readOnly",
    "executionAuthority",
    "effectPerformed",
    "servedAt",
    "snapshotAgeMs",
    "receipt"
  ],
  "properties": {
    "transportSchemaVersion": { "const": "phios.system-state-transport.v1" },
    "transport": { "const": "loopback-http" },
    "transportIdentity": { "const": "phishell-local-observer" },
    "localOnly": { "const": true },
    "readOnly": { "const": true },
    "executionAuthority": { "const": false },
    "effectPerformed": { "const": false },
    "servedAt": { "type": "string", "format": "date-time" },
    "snapshotAgeMs": { "type": "number", "minimum": 0, "maximum": 5000 },
    "receipt": { "$ref": "phios.system-state.v1" }
  },
  "additionalProperties": false
}
