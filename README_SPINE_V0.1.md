# PhiOS Spine v0.1

The first executable proof of the modern PhiOS contract:

**PhiVessel plans → Registry identifies → Policy authorizes → Executor acts → Artifact is hashed → Reality Ledger records.**

See `docs/PHIOS_SPINE_V0.1.md` for the frozen v0.1 contract and examples.
