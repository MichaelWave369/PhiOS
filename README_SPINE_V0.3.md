# PhiOS Spine v0.3 — North Gate / SOMA Perception

Spine v0.3 adds the first executable Perception Gate to the Mandala runtime.

**Native input → provenance → bounded recovery → PerceptionReceipt**

The implementation preserves source bytes, records deterministic transforms, tracks acuity, and refuses to convert perception into truth or authority.

See `docs/PHIOS_SPINE_V0.3_NORTH_GATE.md`.
